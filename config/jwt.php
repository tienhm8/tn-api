<?php

return [

    /*
    |--------------------------------------------------------------------------
    | JWT Secret
    |--------------------------------------------------------------------------
    |
    | Khóa bí mật dùng để ký & xác thực JWT (HS256). Bắt buộc cấu hình.
    |
    */

    'secret' => env('JWT_SECRET'),

    /*
    |--------------------------------------------------------------------------
    | Algorithm & Leeway
    |--------------------------------------------------------------------------
    */

    'algo' => env('JWT_ALGO', 'HS256'),

    'leeway' => (int) env('JWT_LEEWAY', 0),

    /*
    |--------------------------------------------------------------------------
    | Token TTL (phút)
    |--------------------------------------------------------------------------
    |
    | Thời gian sống của access token tính bằng phút. Mặc định 1 ngày.
    |
    */

    'ttl' => (int) env('JWT_TTL', 1440),

];
